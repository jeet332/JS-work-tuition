A function is a block of Organised re-usable for that is used to perform a single related action.
Functions provide better modularity for your application and a high degree of coding re use it.
In python a function define using the
def__

syntax:
def function=name():
       STATEMNET

function.name()-calling funtion